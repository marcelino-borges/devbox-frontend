export enum FirebaseSignInErrors {
  EMAIL_BADLY_FORMATTED = "The email address is badly formatted."
}

export enum SignInMessages {
  VALID_EMAIL = "Please type a valid email!",
  CHECK_CREDENTIALS = "Check your credentials!"
}
